# tw_stocker VPS API 依賴（與 tw_stocker 核心 requirements.txt 分離）
# 核心運算依賴（yfinance / pandas / numpy / matplotlib / exchange_calendars）
# 由 tw_stocker repo 的 requirements.txt 提供；此處只列 API 層所需。

fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
psutil>=5.9
yfinance>=0.2.40
